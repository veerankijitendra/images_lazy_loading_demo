export enum ERoutes {
    landing = "/",
    lazyLoadComponent = "lazy-load",
    lazyLoadImageComponent = "lazy-load-image-componet",
    nativeIntersectionObserver = "native-intersection-observer",
    product = "product"
}